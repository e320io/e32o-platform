import './globals.css';
export const metadata = { title: 'e.32o — Plataforma', description: 'Plataforma integral de gestión' };
export default function RootLayout({ children }) {
  return <html lang="es"><body>{children}</body></html>;
}
