'use client';
// TODO: Vista del equipo
// Este módulo se construye en el Chat 2 del proyecto e.32o (junto con pipeline)
export default function Team() {
  return <div style={{ padding: 40, textAlign: 'center', color: '#888' }}>Vista del equipo — pendiente de construir</div>;
}
